const Tenant = require('./Tenant');
const Config = require('./Config');
const ConfigVersion = require('./ConfigVersion');
const AuditLog = require('./AuditLog');

module.exports = {
    Tenant,
    Config,
    ConfigVersion,
    AuditLog
};
